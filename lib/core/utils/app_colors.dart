//to handel the colors of the app
import 'package:flutter/material.dart';

class AppColors {
  static Color primary = Colors.deepPurple;
  static Color hint = Colors.grey;
  static Color container = const Color.fromARGB(255, 154, 8, 59);
}
